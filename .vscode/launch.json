{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Uwa-Server-Checker Debug",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}",
            "preLaunchTask": "buildenv",
            "envFile": "${workspaceFolder}/.env",
        }
    ]
}